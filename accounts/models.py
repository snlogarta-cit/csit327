from django.db import models
from django.contrib.auth.models import User

# Standard Django built-in User model is used for authentication.
# Additional custom models can be defined here if needed in the future.
