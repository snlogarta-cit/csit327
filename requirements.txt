asgiref==3.12.1
Django==6.1.1
# psycopg3 kept for non-GIS DB usage / future migration
psycopg[binary]==3.3.5
# psycopg2 is required by django.contrib.gis.db.backends.postgis
# cp314 win_amd64 wheel available from PyPI
psycopg2-binary==2.9.13
python-dotenv==1.2.3
sqlparse==0.6.0
tzdata==2026.3

# --- ATM Social Map dependencies ---
# DRF 3.18.1 requires Django>=5.2 — compatible with Django 6.1 and DRF-GIS 1.3.0
djangorestframework==3.18.1
djangorestframework-gis==1.3.0

# --- GDAL / GEOS on Windows ---
# The pip 'GDAL' package does not have a pre-built wheel for Python 3.14.
# Install GDAL native libraries via OSGeo4W instead:
#   https://trac.osgeo.org/osgeo4w/
# Then set the paths in settings.py:
#   GDAL_LIBRARY_PATH = r'C:\OSGeo4W\bin\gdal310.dll'
#   GEOS_LIBRARY_PATH = r'C:\OSGeo4W\bin\geos_c.dll'
#   PROJ_LIB          = r'C:\OSGeo4W\share\proj'
